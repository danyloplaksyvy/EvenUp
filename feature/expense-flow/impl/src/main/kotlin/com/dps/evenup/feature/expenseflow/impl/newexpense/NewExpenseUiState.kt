package com.dps.evenup.feature.expenseflow.impl.newexpense

data class NewExpenseUiState(
    val title: String = "Add expense",
    val helperText: String = "Split shared expenses without the awkward math.",
)
