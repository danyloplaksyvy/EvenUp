{
  "name": "evenup-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test",
    "typecheck": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}
